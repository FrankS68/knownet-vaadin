services:
  neo4j:
    image: neo4j:5
    container_name: knownet-neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      - NEO4J_AUTH=neo4j/knownet123
    volumes:
      - neo4j-data:/data
    restart: unless-stopped

volumes:
  neo4j-data:
